"""
集中配置

Phase 2b 模块的全局配置常量：存储路径、默认参数等。

路径约定:
    - PROJECT_ROOT 使用与 data_infra 一致的推导方式
    - 信号存储: db/signals/
    - 模型存储: db/models/
"""

from pathlib import Path


# ---------------------------------------------------------------------------
# 项目根目录
# ---------------------------------------------------------------------------
# config.py 在 alpha_model/ 下，往上一级即为项目根目录
PROJECT_ROOT = Path(__file__).resolve().parent.parent

# ---------------------------------------------------------------------------
# 存储路径
# ---------------------------------------------------------------------------
DB_DIR = PROJECT_ROOT / "db"

# SignalStore 默认路径: db/signals/{strategy_name}/
SIGNAL_STORE_DIR = DB_DIR / "signals"

# ModelStore 默认路径: db/models/{model_name}/
MODEL_STORE_DIR = DB_DIR / "models"

# ---------------------------------------------------------------------------
# 默认交易对
# ---------------------------------------------------------------------------
DEFAULT_SYMBOLS = [
    "BTC/USDT",
    "DOGE/USDT",
    "SOL/USDT",
    "BNB/USDT",
    "ETH/USDT",
]

# ---------------------------------------------------------------------------
# 回测默认参数
# ---------------------------------------------------------------------------
# Binance taker 手续费率（单边）
DEFAULT_FEE_RATE = 0.0004

# 市场冲击系数（square-root model）
DEFAULT_IMPACT_COEFF = 0.1

# 默认组合总资金（USDT）
DEFAULT_PORTFOLIO_VALUE = 10000.0

# ---------------------------------------------------------------------------
# 年化常数
# ---------------------------------------------------------------------------
# 1 分钟 K 线：一年约 365.25 × 24 × 60 = 525,960 根
MINUTES_PER_YEAR = 525_960
